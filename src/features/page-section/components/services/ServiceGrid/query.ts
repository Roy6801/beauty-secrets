const responseStructure = `
  sectionId,
  title,
  highlightText,
  titleSuffix,
  description,
  services[] {
    name,
    description,
    price,
    icon,
    learnMoreLink
  }
`;

export default responseStructure;
